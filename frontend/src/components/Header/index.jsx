import { SectionContainer } from '../../styles';

const Header = () => {
  return (
    <header>
      <SectionContainer>
        <h1>Header</h1>
      </SectionContainer>
    </header>
  );
}

export default Header;